# Leaf-Health-Analyzer
Plant disease classification using transfer learning on leaf image datasets. The project trains and evaluates CNN models to detect diseased vs healthy leaves and studies cross-dataset generalization between controlled lab images and real-world field photos.
